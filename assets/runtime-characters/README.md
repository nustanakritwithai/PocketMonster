# Studio runtime characters

Place Character Prototype Studio V1.9.0 exports here as:

`<name>.pocket-character.json`

Then open the V9 world with:

`?studioCharacter=/PocketMonster/assets/runtime-characters/<name>.pocket-character.json`

Packages remain presentation-only and are validated by the V9.1 `studio-character` package gate before live spawn.
