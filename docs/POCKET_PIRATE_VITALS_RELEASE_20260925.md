# Pocket × Pirate vitals / economy integration — reviewed dependency

## Exact native source

- Native source: `nustanakritwithai/Pirate-fruit-@c8c725e8a12e6723a8015e74566965ae18218ff2`.
- Upstream PR #168 merged as `eeec6e626e7b542d2229ffd909ffb5c015c2f111` after its exact-head client/server, audio and renderer gates passed.
- The generated Pocket dependency update is commit `35d769ef56c7408d1a1ae1f40fd072a6559dbab1`.
- Native entry: `pirate-fruit-offline/assets/index-D3UrpAn_.js`.
- SHA-256 of native entry: `1e79f3e0dae4c2b8865aae6393e30f43517ffe5982dd125d77beb4f2a9703f4e`.
- Existing save-sandbox, presentation, native input and fullscreen boot hooks are preserved.
- The independent source pins in acceptance and the post-deploy verifier advance together with the generated bytes; they remain exact assertions.

## Runtime review and repairs

The native quickslot path previously routed HP potions to the server but still consumed MP potions locally. The reviewed dependency routes both resources through the authoritative operation path, including rejected, unavailable and failed requests. New tests exercise the public hotkey update path and keep offline behavior unchanged.

The parent vitals ingress now rejects contradictory death/HP values, zero maximum HP, malformed respawn objects and empty respawn identifiers before forwarding to the native receiver.

## Evidence and its scope

The Project Brain behavioral pair gate `36071452541` passed eight groups using Pocket `f0a776449a0ef9b917fb0bcbaee952a44c343816` and native `c8c725e8a12e6723a8015e74566965ae18218ff2`:

1. Real server vitals producer → Pocket relay → native monotonic receiver.
2. Parent rejection of malformed life/respawn fields.
3. HP and MP hotkeys never debit locally under server authority.
4. Native potion emitter → Pocket sanitizer → real server operation, idempotent replay and key collision.
5. Server respawn → parent → one presentation teleport.
6. Real EconomyEngine quote, execute, replay and key collision.
7. Session-bound HTTP client and transient input outside the persistent queue.
8. Transport retry identity and bounded CAS conflict rebase.

The native refresh also ran the complete `test:v90:pirate-player` chain and unified Pages artifact checks before committing generated files. Subsequent PR CI must pass again on the final head before merge.

The behavioral gate uses real domain modules but injects the HTTP/SQL host boundary. It is not a production host or live authenticated-user proof. Generated dependency provenance retains this distinction. Production deployment must pass the existing backend compatibility and post-deploy hash gates; this change enables no production feature flag.

## Build tooling

`scripts/refresh-pirate-native.mjs` preserves the deterministic refresh recipe. The temporary branch-write workflow was removed after its bounded dependency-generation job completed; no recurring bot write workflow is installed.
