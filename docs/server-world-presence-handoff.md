ถึงทีมเซิร์ฟเวอร์ world presence ยังไม่ตามตัวผู้เล่น เทส production 2 เซสชัน Guest ปกติ+Guest Incognito ที่ pocketmonster-game.web.app แล้วเข้า github.io/PocketMonster แชทช่อง WORLD ส่งถึงกันได้บนซ็อกเก็ตเดียวกัน แต่ป้ายผู้เล่นในโลกไม่ตามตำแหน่งจริง คลายเอนต์มี WORLD_STATE และส่งตำแหน่งทุก 250ms หลังออธซ็อกเก็ต ตำแหน่งตัวเองขยับตามการเดิน แต่ทั้งสองจอเห็นป้าย guest ค้างที่ x=8 z=5 ซึ่งไม่ใช่พิกัดคนที่เทส (เดินไปประมาณ -10,12 และ -10,5) เกมยังไม่มีโมเดล 3D ของคนอื่น มีแค่ป้าย HTML จากสแนปช็อต อย่าใส่ token cookie Firebase UID launch ticket หรือป้าย Guest+hex ลงในล็อก
เวอร์ชันตอนเทส คลายเอนต์ 8.4.0-github.181f489 API 8.4.0-world-presence.20260829 commit bb110940 features มี chatWebSocket ยังไม่มี worldPresence คลายเอนต์ปิด vpsWrites/playerDataWrites แชทยังส่งได้และ presence เดินบน WebSocket อย่าผูกกับธงเขียนเซฟ
สัญญาที่คลายเอนต์ใช้จริงที่ wss://157.85.96.139/ws/chat เฟรมแรก { "token": "<session token>" } จากนั้นทุก 250ms { "type": "world-pos", "zone": "hub", "x": 0.0, "z": 0.0, "dir": 0.0 } Ranch Hub ใช้โซนสตริง hub ตรงๆ ไม่ใช่ Ranch Hub x/z เป็นตัวเลขโลก Three.js dir คือ player.rotation.y ส่งมาแล้วแต่ยังไม่ใช้วาดป้าย
คลายเอนต์รับเฉพาะ { "type": "world-snapshot", "payload": { "zone": "hub", "players": [{ "id": "stable-id", "name": "display name", "x": 1.25, "z": -3.5 }] } } ทิ้งทั้งเฟรมเงียบๆ ถ้า type ไม่ใช่ world-snapshot หรือไม่มี payload (ส่ง zone/players ที่รูทไม่ได้) หรือ payload.zone ไม่เท่ากับ hub แบบเทียบสตริงตรง หรือคนในลิสต์ไม่มี id หรือ x/z ไม่ใช่ตัวเลข finite name ถ้าไม่มีจะโชว์ ผู้เล่นออนไลน์ คนที่หายจาก players จะถูกลบป้าย แชทบนซ็อกเก็ตเดิมยังต้องยิง { "type": "chat" } อย่าทำให้เฟรมแชทพัง
ขอให้ตรวจ (1) รับ world-pos หลังออธแล้วอัปเดตทุกครั้งไม่ใช่แค่ครั้งแรก (2) อย่าล็อกทุกคนไว้ที่สปอว์น 8,5 (3) ส่งคนอื่นในโซนเดียวกันไม่รวมตัวผู้รับ (4) อัปเดตสดต้องห่อ payload เหมือนสแนปช็อตแรก (5) Guest/anonymous ต้องเข้า presence ได้เพราะแชทผ่านแล้วโทเคนใช้กับซ็อกเก็ตได้ (6) บอทอย่าง Atlas หรือเซสชันค้างอย่าไปแทนที่คนจริงในลิสต์ (7) หลุดซ็อกเก็ตแล้วลบออกจากสแนปช็อตมี TTL (8) throttle ส่งออกได้ประมาณ 200-500ms แต่ค่าที่เก็บต้องเป็นตำแหน่งล่าสุด (9) ถ้าพร้อมแล้วใส่ worldPresence ใน GET /api/version features
พิสูจน์ฝั่งเซิร์ฟเวอร์ มีเฟรม world-pos สองไอดีในโซน hub หรือไม่ ให้ A เดินจน x หรือ z เปลี่ยนอย่างน้อยประมาณ 8 หน่วยแล้วสแนปช็อตของ B ต้องมี A ที่พิกัดใหม่ไม่ใช่ 8,5 สแนปช็อตของ A ต้องมี B ที่พิกัดจริง payload.zone ต้องเป็น hub ตัดซ็อกเก็ต A แล้ว A ต้องหายจากสแนปช็อตของ B ภายในไม่กี่วินาที ถ้าไม่มีเฟรมเข้า=ยังไม่ parse world-pos ถ้ามีเฟรมแต่พิกัดค้าง=เก็บครั้งแรกแล้วยังไม่อัปเดตหรือกระจายคนละคน ถ้าเซิร์ฟเวอร์ถูกแต่เกมไม่ขยับ=shape ไม่ใช่ { type, payload } หรือโซนไม่ตรง hub
คลายเอนต์ตอนนี้ไม่ส่งโมเดล 3D/สกินไปวาดคนอื่น ไม่กรองตามระยะแค่เทียบโซน ไม่โชว์ error ถ้าสแนปช็อตผิดรูป และไม่มี REST สำหรับ presence มีแต่ซ็อกเก็ตแชทเส้นเดียว
